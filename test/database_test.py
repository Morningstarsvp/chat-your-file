# from database import *
#
# conversation = schemas.Conversation(name="default", chat_type="llm_chat")
# conversation_id = crud.create_conversation(conversation)
# print(conversation_id)
import uuid

id = uuid.uuid4().hex
print(id)
id = uuid.uuid4().hex
print(id)